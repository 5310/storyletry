{
  "name": "storylets",
  "version": "0.1.0",
  "scripts": {
    "lint": "npx tslint -p .",
    "build": "npm run lint && npx @pika/pack"
  },
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-ts-standard-pkg"
      ],
      [
        "@pika/plugin-build-node"
      ]
    ]
  },
  "devDependencies": {
    "@pika/pack": "^0.5.0",
    "@pika/plugin-build-node": "^0.9.2",
    "@pika/plugin-ts-standard-pkg": "^0.9.2",
    "typescript": "^3.8.3",
    "typescript-tslint-plugin": "^0.5.5"
  }
}